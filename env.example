# Base mainnet private key (hex, starts with 0x) - NEVER commit this!
PRIVATE_KEY=

# Your Base wallet address (for logging/reference)
WALLET_ADDRESS=

# Optional: mnemonic instead of private key
# MNEMONIC="word1 word2 ... word12"

# API base (change if needed)
API_BASE_URL=https://bounty.owockibot.xyz